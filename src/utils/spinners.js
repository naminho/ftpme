import ora from 'ora'

export const install = ora('Installing package dependencies...')
export const build = ora('Building assets...')
export const upload = ora('Uploading assets to FTP server...')
export const connecting = ora('Establising connection to the FTP server...')
